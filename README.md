# StoreFlow---Smart-Restocking-Workflow
I observed a customer’s weekly ordering process, identified that the existing POS lacked SKU-level inventory tracking, created an integration around the legacy workflow, piloted it with one supplier, measured recommendation accuracy, trained the user, and iterated based on real operational feedback.
